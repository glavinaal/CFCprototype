@model CFCprototype.Models.Sermon

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Create</title>
</head>
<body>

<h4>Sermon</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SermonTitle" class="control-label"></label>
                <input asp-for="SermonTitle" class="form-control" />
                <span asp-validation-for="SermonTitle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SermonLink" class="control-label"></label>
                <input asp-for="SermonLink" class="form-control" />
                <span asp-validation-for="SermonLink" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SermonDate" class="control-label"></label>
                <input asp-for="SermonDate" class="form-control" />
                <span asp-validation-for="SermonDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
</body>
</html>
