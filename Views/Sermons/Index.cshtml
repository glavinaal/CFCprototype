@model IEnumerable<CFCprototype.Models.Sermon>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SermonTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SermonLink)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SermonDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SermonTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SermonLink)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SermonDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SermonID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SermonID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SermonID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
