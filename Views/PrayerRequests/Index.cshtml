@model IEnumerable<CFCprototype.Models.PrayerRequest>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RequestSubject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestBody)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RequestSubject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestBody)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PrayerRequestID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PrayerRequestID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PrayerRequestID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
